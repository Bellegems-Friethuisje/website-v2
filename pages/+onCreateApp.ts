import type { OnCreateAppSync } from "vike-vue/types";

export const onCreateApp: OnCreateAppSync = (_pageContext): ReturnType<OnCreateAppSync> => {
  // Google Analytics can be added here once a tag ID is configured
};
